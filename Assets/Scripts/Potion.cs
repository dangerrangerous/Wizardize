using UnityEngine;
using System.Collections;

public class Potion : MonoBehaviour {

    int numberOfPotionTypes;
    // create a queue
    Queue potions;

    // have each type of potion in queue

    // spawn potion in position 0 of queue

    // dequeue enqueue (pop push etc)

    // apply potion modifiers. start with speed as it will be easiest. but first... eat!

    /*class SpeedPotion
    {
        private readonly PlayerMoveSpeed speed;

        public SpeedPotion(PlayerMoveSpeed speed)
        {
            this.speed = speed;
        }

        private void SpeedIncreaser()
        {
            float moveSpeed = speed.Movespeed;
            speed.MoveSpeed = 7;
        }
    }
    */

    // Use this for initialization
    void Start () {
	

	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
